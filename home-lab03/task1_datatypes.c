#include <stdio.h>
#include <stdbool.h>
int main() {
int age=20;
  float height=5.6f;
double pi=3.1415926;
char grade ='a';
bool is student = true;
printf("int value:%d,size:%zu.bytes\n",age,size of(age));
  printf("float value:%2f,size:5zu bytes\n",height,size of(height));
printf("double value :%.14f,size:5zu bytes\n",pi,size of (pi));
printf("char value :%c,size:%zu bytes\n",grade,sizeof(grade));
printf("bool value:%d,size:%zu bytes\n",is student,
  size of(is student));
return 0;
}
